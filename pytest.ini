[pytest]
# Запуск из корня репозитория: .venv/bin/python -m pytest (или активированный venv).
testpaths =
    tests
norecursedirs =
    tests/dev
addopts =
    --cov=src --cov-report=term-missing --cov-report=xml -ra --maxfail=5 --disable-warnings

